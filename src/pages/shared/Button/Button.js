import React from 'react';
import './Button.css'

const Button = () => {
    return (
        <div>
            <button class="button type1">
                <span class="btn-txt">Donation Now</span>
                </button>
        </div>
    );
};

export default Button;